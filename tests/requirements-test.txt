# Test-only dependencies for the SARMAAN / ERITAS MDA QA suite.
# Install inside the api container:  pip install -r tests/requirements-test.txt
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.0
anyio==4.4.0
# asyncpg, sqlalchemy, fastapi, bcrypt, pyjwt, slowapi, redis, sentry-sdk
# are already provided by the application image (requirements.txt).
